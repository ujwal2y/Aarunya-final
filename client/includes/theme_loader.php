<?php
/**
 * Theme Loader - intentionally empty.
 * premium-design-system.css (loaded before this) defines all CSS variables.
 * Do NOT load variables.css here — it overrides --primary to teal and breaks the theme.
 */
?>
